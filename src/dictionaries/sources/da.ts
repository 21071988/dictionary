import type { DictionarySource } from '../types';

/**
 * Empty on purpose — paste real entries into `words` below. Shape of one
 * entry (word = Danish, translation = your own language, transcription is
 * optional pronunciation help):
 *
 * { word: 'hund', translation: 'dog', transcription: 'hun' }
 */
export const daBasics: DictionarySource = {
  id: 'da-basics',
  language: 'da',
  name: 'Danish basics',
  words: [],
};
