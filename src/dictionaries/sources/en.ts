import type { DictionarySource } from '../types';

/**
 * Empty on purpose — paste real entries into `words` below. Shape of one
 * entry (word = English, translation = your own language, transcription is
 * optional pronunciation help):
 *
 * { word: 'dog', translation: 'perro', transcription: 'dɒg' }
 */
export const enBasics: DictionarySource = {
  id: 'en-basics',
  language: 'en',
  name: 'English basics',
  words: [],
};
